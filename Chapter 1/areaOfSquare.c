#include<stdio.h>
int main(){
    float side;
    printf("Enter the side of the square : \n");
    scanf("%f", &side);
    float area ;
    area = side * side;
    printf("Area of the Square is %f", area);
}